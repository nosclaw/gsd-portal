:80 {
  encode gzip zstd

  # GSD workspace proxy — routes /workspace/* through Portal's proxy endpoint
  # Portal performs server-side auth and resolves the target GSD port
  handle /workspace/* {
    reverse_proxy portal:3000
  }

  # All other traffic goes to Portal
  handle {
    reverse_proxy portal:3000
  }
}
